import { DirectivepropertybindingDirective } from './directivepropertybinding.directive';

describe('DirectivepropertybindingDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivepropertybindingDirective();
    expect(directive).toBeTruthy();
  });
});
